apiVersion: v1
kind: ReplicationController
metadata:
  name: pithos
  labels:
    app: pithos
    pithos-role: pithos
spec:
  replicas: 1
  selector:
    app: pithos
    pithos-role: pithos
  nodeSelector:
    pithos-role: node
  template:
    metadata:
      labels:
        app: pithos
        pithos-role: pithos
    spec:
      hostNetwork: true
      containers:
      - image: pithos:latest
        name: pithos
        ports:
          - containerPort: 8080
            name: pithos
        volumeMounts:
          - name: pithos-cfg
            mountPath: /etc/pithos
      volumes:
        - name: pithos-cfg
          configMap:
            name: pithos-cfg
